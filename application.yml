server:
  port: 2333
  address: 0.0.0.0

lavalink:
  plugins:
    - dependency: "dev.lavalink.youtube:youtube-plugin:1.18.1"
      repository: "https://maven.lavalink.dev/releases"

  server:
    password: "crystalmusic123"
    sources:
      youtube: false # Set to false because the plugin handles it
      bandcamp: true
      soundcloud: true
      twitch: true
      vimeo: true
      http: true
      local: false

plugins:
  youtube:
    enabled: true
    allowSearch: true
    allowDirectVideoIds: true
    allowDirectPlaylistIds: true

    # Fixed: Using OAuth-compatible clients and removed invalid ones
    clients:
      - TVHTML5
      - ANDROID
      - IOS
      - WEB

    oauth:
      enabled: true
      # Your existing refresh token
      refreshToken: "1//05WTnyBUg67K5CgYIARAAGAUSNwF-L9IrzPoCXRZRF0-d9tozyAZZw52hCchHSSTYhqgF0k3wEkrInoKo97sfCrUgiwjmpPOV3_c"

    # Optional: If playback still fails, you can try commenting this out
    cipherServerUrl: "https://yt-cipher.kikkia.dev"

logging:
  level:
    root: INFO
    lavalink: INFO
